import React from 'react';
import ReactDOM from 'react-dom/client';
import TimekeepingDashboard from './TimekeepingDashboard.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <TimekeepingDashboard />
    </React.StrictMode>
);
